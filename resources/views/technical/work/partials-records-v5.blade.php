<div class="ego-doc-table-wrap"><table class="ego-doc-table"><thead><tr><th>Công trình</th><th>Công việc</th><th>Ngày</th><th>Nhân sự</th><th>Trạng thái</th></tr></thead><tbody>@forelse($records->take(20) as $r)<tr><td>{{ $r->project_code ?? ('CT-'.$r->site_id) }} · {{ $r->site_name }}</td><td>{{ $workTypeLabels[$r->work_type] ?? $r->work_type }}</td><td>{{ \Illuminate\Support\Carbon::parse($r->work_date)->format('d/m/Y') }}</td><td>{{ implode(', ', $r->assignee_names ?? []) ?: '—' }}</td><td><span class="ego-state-pill">{{ $r->status==='completed' ? 'Hoàn thành' : ($r->status==='reported' ? 'Đã báo cáo' : 'Kế hoạch') }}</span></td></tr>@empty<tr><td colspan="5" class="ego-empty-cell">Chưa có công việc Kỹ thuật.</td></tr>@endforelse</tbody></table></div>
