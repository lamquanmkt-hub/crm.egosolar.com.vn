@extends('layouts.app')
@section('title', 'Chi tiết tạm ứng lương')
@push('styles')<link rel="stylesheet" href="{{ asset('css/ego-payment-advance-modules.css') }}?v=1">@endpush
@section('content')
<div class="container-fluid ego-advance-page">
<div class="ego-advance-head"><div><a href="{{ route('salary_advances.index') }}" class="text-decoration-none small"><i class="bi bi-arrow-left"></i> Danh sách DNTUL</a><h1 class="ego-advance-title mt-2">{{ $item->code }}</h1><div class="ego-advance-sub">Tạm ứng lương kỳ {{ $item->payroll_month }}</div></div><span class="ego-advance-status {{ $item->status==='accounting_approved'?'is-good':(str_contains($item->status,'rejected')?'is-bad':'is-warn') }}">{{ $statusLabels[$item->status] ?? $item->status }}</span></div>
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
@if(session('error'))<div class="alert alert-danger">{{ session('error') }}</div>@endif
<div class="ego-advance-detail-grid"><div><div class="ego-advance-card"><div class="ego-advance-card__head"><strong>Thông tin đề nghị</strong><div class="ego-advance-actions">@if($isOwner && in_array($item->status,['draft','admin_rejected','accounting_rejected'],true))<form method="POST" action="{{ route('payment_requests.submit',$item->payment_request_id) }}">@csrf<button class="btn btn-sm btn-primary">Gửi duyệt</button></form>@endif @if($isAdmin && $item->status==='submitted')<form method="POST" action="{{ route('payment_requests.admin_approve',$item->payment_request_id) }}">@csrf<button class="btn btn-sm btn-success">GĐ duyệt</button></form><button class="btn btn-sm btn-outline-danger" data-bs-toggle="modal" data-bs-target="#adminRejectModal">Từ chối</button>@endif @if($isAccounting && $item->status==='admin_approved')<form method="POST" action="{{ route('payment_requests.acc_approve',$item->payment_request_id) }}">@csrf<button class="btn btn-sm btn-success">Xác nhận đã chi</button></form><button class="btn btn-sm btn-outline-danger" data-bs-toggle="modal" data-bs-target="#accRejectModal">Từ chối</button>@endif</div></div><div class="ego-advance-card__body"><div class="ego-advance-info"><div class="ego-advance-info-item"><small>Nhân viên</small><strong>{{ $item->employee_name ?: '—' }}</strong></div><div class="ego-advance-info-item"><small>Kỳ lương</small><strong>{{ $item->payroll_month }}</strong></div><div class="ego-advance-info-item"><small>Số tiền tạm ứng</small><strong class="ego-advance-money">{{ number_format($item->requested_amount) }} đ</strong></div><div class="ego-advance-info-item"><small>Ngày cần nhận</small><strong>{{ $item->needed_date ? \Carbon\Carbon::parse($item->needed_date)->format('d/m/Y') : '—' }}</strong></div><div class="ego-advance-info-item"><small>Ngân hàng</small><strong>{{ $item->bank_info ?: '—' }}</strong></div><div class="ego-advance-info-item"><small>Ghi vào bảng lương</small>@if($item->applied_to_payroll_at)<strong class="text-success">Đã ghi nhận</strong><div class="small text-muted">{{ \Carbon\Carbon::parse($item->applied_to_payroll_at)->format('d/m/Y H:i') }}</div>@else<strong>Chưa ghi</strong>@endif</div></div><div class="mt-3"><small class="text-muted">Lý do</small><div>{{ $item->reason }}</div></div></div></div></div><div><div class="ego-advance-card mb-3"><div class="ego-advance-card__head"><strong>Luồng duyệt</strong></div><div class="ego-advance-card__body">@forelse($approvals as $a)<div class="ego-advance-step"><div class="ego-advance-step__dot"><i class="bi bi-check2"></i></div><div><strong>{{ $a->actor_name ?: 'Hệ thống' }}</strong><div class="small">{{ ucfirst($a->step) }} · {{ $a->action }}</div><div class="small text-muted">{{ $a->created_at ? \Carbon\Carbon::parse($a->created_at)->format('d/m/Y H:i') : '' }}</div>@if($a->note)<div class="small mt-1">{{ $a->note }}</div>@endif</div></div>@empty<div class="text-muted">Chưa có lịch sử duyệt.</div>@endforelse</div></div>@if($attachments->count())<div class="ego-advance-card"><div class="ego-advance-card__head"><strong>Chứng từ</strong></div><div class="ego-advance-card__body">@foreach($attachments as $f)<div class="mb-2"><a target="_blank" href="{{ Storage::url($f->path) }}"><i class="bi bi-paperclip"></i> {{ $f->original_name }}</a></div>@endforeach</div></div>@endif</div></div></div>
<div class="modal fade" id="adminRejectModal" tabindex="-1"><div class="modal-dialog"><div class="modal-content"><form method="POST" action="{{ route('payment_requests.admin_reject',$item->payment_request_id) }}">@csrf<div class="modal-header"><h5 class="modal-title">Từ chối phiếu</h5><button class="btn-close" data-bs-dismiss="modal"></button></div><div class="modal-body"><textarea required name="note" class="form-control" rows="4"></textarea></div><div class="modal-footer"><button class="btn btn-danger">Xác nhận</button></div></form></div></div></div>
<div class="modal fade" id="accRejectModal" tabindex="-1"><div class="modal-dialog"><div class="modal-content"><form method="POST" action="{{ route('payment_requests.acc_reject',$item->payment_request_id) }}">@csrf<div class="modal-header"><h5 class="modal-title">Kế toán từ chối</h5><button class="btn-close" data-bs-dismiss="modal"></button></div><div class="modal-body"><textarea required name="note" class="form-control" rows="4"></textarea></div><div class="modal-footer"><button class="btn btn-danger">Xác nhận</button></div></form></div></div></div>
@endsection
